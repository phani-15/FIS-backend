<!DOCTYPE html>
<html>
<head>
  <title>Biodata</title>
  <style>
    body { font-family: Arial, sans-serif; margin: 40px; }
    h1 { text-align: center; }
    .section { margin-bottom: 20px; border: 2px; }
    .label { font-weight: bold; }
  </style>
</head>
<body>
  <h1>Biodata</h1>
  <div class="section"><span class="label">Name:</span> <%= user.name %></div>
  <div class="section"><span class="label">Age:</span> <%= user.age %></div>
  <div class="section"><span class="label">Email:</span> <%= user.email %></div>
  <div class="section"><span class="label">Phone:</span> <%= user.phone %></div>
  <div class="section"><span class="label">Education:</span> <%= user.education %></div>
  <div class="section"><span class="label">Skills:</span> <%= user.skills.join(", ") %></div>
</body>
</html>
